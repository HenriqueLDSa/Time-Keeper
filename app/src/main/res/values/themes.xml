<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ClickCounter" parent="Theme.AppCompat">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ClickCounter" parent="Base.Theme.ClickCounter" />

    <style name="CustomNumberPicker">
        <item name="colorControlNormal">@color/black</item> <!-- For the arrows -->
        <item name="colorControlActivated">@color/black</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/text_color</item>
    </style>
</resources>